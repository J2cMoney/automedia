# 设计规范:自媒体全自动运营流水线

> 视觉契约(一句话):**深色底(近黑/深灰)+ 克制蓝紫强调色 + 紧凑信息密度 + 标准四态色 + 无衬线字体 + snappy 隐形动效。气质对标 Vercel dashboard:冷、精准、开发者向的专业感。绝不宽松留白。**
>
> 本文为单一视觉真相源。设计稿、代码样式均以此为据。Product-Spec.md 是功能范围的事实来源,本文不新增 Spec 外功能。

---

## 0. AI 使用说明

- 本文档定义产品的界面结构、视觉系统、组件、状态和交互规则。
- Product-Spec.md 是功能范围的事实来源;本文档不得新增 Spec 没有的核心功能。
- 形态为界面型,填 §2 §3 §5 §8.3,§A 终端/Agent 轨标 N/A。
- AI MUST 优先设计 P0 flows 和 P0 screens(SCREEN-1 仪表盘、SCREEN-3 内容流水线、SCREEN-2 账号管理)。
- AI MUST 覆盖默认、加载、空、错误、成功、无权限六态。
- AI MUST 保持页面、组件、交互、视觉 token 的一致性。
- SCREEN / CMP 编号呼应 Product-Spec.md 的 FLOW/REQ,两边 P0 内容一致。

---

## 1. 设计方向

### 1.0 产品形态

| 维度 | 方向 |
|---|---|
| 形态 | **界面型**(Web 面板,单形态,无终端/Agent 轨) |
| 本体 | 页面与组件 |
| 落地分轨 | 用 §2 §3 §5 §8.3;§A 标 N/A |

### 1.1 设计目标

让一人公司运营者打开面板 3 秒内获得**省心感**:一眼看到所有账号状态、哪个任务在跑、哪个号出问题。视觉服务于"快速定位 + 最小操作触发",不服务于装饰。

### 1.2 产品气质

| 维度 | 方向 |
|---|---|
| 语气 | 冷静、精准、可靠(不亲切、不活泼、不说教) |
| 信息密度 | **紧凑**(compact)——10 个号 × 多环节要一屏扫完,留白是奢侈品 |
| 视觉风格 | 现代 SaaS / 工具型 Dashboard / 控制台感 |
| 情绪目标 | **省心感**——"它替我干活了,我只要看结果",不是"掌控感"也不是"高效感" |

### 1.3 设计原则

- **DP-001 密度优先于装饰**:屏幕空间是稀缺资源,任何装饰(插画/渐变背景/动效)都要为信息让路。一屏多看一个号 > 一屏多一个漂亮空状态。
- **DP-002 状态色即语言**:绿/黄/红/蓝四态色是面板的主语义,用户靠颜色扫一眼定位问题,不用读字。颜色不滥用,只有状态用色。
- **DP-003 动效隐形**:动效只为反馈状态存在(进度、切换),绝不吸引眼球。转圈是为了告诉用户"在跑",不是为了好看。

### 1.4 参考与反参考

**正向参考:**

| 参考产品 | 具体喜欢哪一点(拆成属性) | 必须保留 |
|---|---|---|
| **Vercel dashboard** | 深近黑底 + 克制蓝紫强调色 + 信息分层清晰 + 状态色克制 + 最小装饰 + 项目卡片网格布局 | Yes(主锄) |
| Linear | 近黑深色 + 精致无衬线字体 + snappy 隐形动效 | Yes(字体/动效参考) |
| Supabase dashboard | 表格密集 + 数据为主 + 侧边深色导航 | Yes(数据页参考) |

**反参考:**
- **Notion / Stripe 浅色大面积留白**:太宽松,一屏放不下 10 个号 + 6 环节,信息密度太低。绝对不要。
- **Canva / 多邻国式活泼多色**:多色渐变、插画、表情符号、庆祝动画。太热闹,不符合省心冷静气质。
- **数据大屏式花哨**:堆满图表、每块闪烁、背景装饰图案。视觉噪点,看着累。
- **纯黑绿黑客终端**:裸数据无设计感。太裸,不像专业产品。

---

## 2. 信息架构

### 2.1 导航结构

**左侧固定侧边栏(深色,可收起)+ 顶部全局操作栏。**

侧边栏导航项(对应 SCREEN):
1. 仪表盘(SCREEN-1)— 默认首页
2. 内容流水线(SCREEN-3)
3. 账号管理(SCREEN-2)
4. 评论中心(SCREEN-4)
5. 数据概览(SCREEN-5)
6. 日志与设置(SCREEN-6)

顶部操作栏:全局"开始今日运营"主按钮(CTA,蓝紫强调色)+ 通知铃铛(异常告警)+ 主题切换(深/浅)。

### 2.2 页面清单

| 页面编号 | 页面名称 | 页面目的 | 关联流程 | 优先级 |
|---|---|---|---|---|
| SCREEN-1 | 仪表盘 | 一眼看全局:账号状态 + 今日任务 + 异常 | FLOW-1/2/3/4/5/8 | P0 |
| SCREEN-3 | 内容流水线 | 看全链路进度,选题→发布各环节状态 | FLOW-1/2/3/4 | P0 |
| SCREEN-2 | 账号管理 | 矩阵 CRUD、登录态、主题配置 | FLOW-6 | P0 |
| SCREEN-4 | 评论中心 | 各账号评论与 AI 回复记录 | FLOW-5 | P0 |
| SCREEN-5 | 数据概览 | 基础数据回显 | NON-7 边界 | P1 |
| SCREEN-6 | 日志与设置 | 任务日志、AI/风控配置 | FLOW-8 | P1 |

### 2.3 页面关系

主路径:仪表盘 →(点某账号/任务)→ 内容流水线 →(点异常)→ 日志。
辅助路径:账号管理 →(配置)→ 仪表盘;评论中心 ↔ 仪表盘。
仪表盘是枢纽,所有页可回到仪表盘。

---

## 3. 页面规格

### SCREEN-1: 仪表盘(P0 核心)

**页面目的:** 用户打开就看到全局——所有账号当前状态、今日任务进度、异常告警。3 秒获得省心感。

**主要操作:** "开始今日运营"(全局 CTA,顶部)

**次要操作:** 点账号卡片跳详情;点异常红点查看;刷新

**布局结构:**
1. 顶部操作栏:全局 CTA + 通知 + 主题切换
2. 顶部摘要条:今日已发布数 / 进行中数 / 异常数(紧凑数字+状态色点)
3. **主内容区:账号卡片网格**(主体,用户已选)
   - 每个账号一张卡片,卡片内:头像/昵称、平台图标、状态色点(绿正常/黄警告/红失败/蓝运行中)、今日任务进度迷你条、最后产出时间
   - 网格响应式:桌面 3-4 列,平板 2 列,移动 1 列
4. 右侧/底部:异常告警列表(可折叠,有异常才显眼)

**内容层级:**

| 优先级 | 元素 | 说明 |
|---|---|---|
| 1 | 账号状态色点 + 状态文字 | 一眼定位哪个号挂了 |
| 2 | 任务进度迷你条 | 知道在跑到哪一步 |
| 3 | 最后产出时间 | 判断是否停滞 |

**使用的组件:** CMP-001 账号卡片、CMP-002 状态徽章、CMP-003 进度条、CMP-004 统计数字、CMP-005 CTA 按钮

**必需状态:**
- 默认:各账号正常状态卡片网格
- 加载:骨架屏(卡片轮廓占位,不转圈)
- 空状态(无账号):极简文案"还没有账号,去账号管理添加第一个" + 跳转链接
- 错误(某账号登录失效):该卡片红点 + "需重新登录",其余卡片正常
- 成功:CTA 点击后,卡片状态色变为蓝色"运行中"
- 无权限(后端未启动):全屏极简文案"请先启动后端服务"

**响应式表现:** 桌面 3-4 列网格;平板 2 列;移动单列堆叠。[移动端为次要场景,详见 8.3]

### SCREEN-3: 内容流水线(P0 核心)

**页面目的:** 看单条内容从选题到发布的全链路进度,定位卡在哪个环节。

**主要操作:** 启动/暂停链路、查看某环节产出、重试失败环节

**布局结构:**
1. 顶部:选题来源 + 状态摘要
2. **主内容:横向流水线条**(热点→文案→脚本→剪辑→分发→回评)
   - 每个环节一个节点,节点含图标+状态色+耗时
   - 当前环节高亮(蓝色),已完成(绿色),失败(红色+错误摘要+重试按钮),待执行(灰色)
3. 分发节点展开:3 平台各自状态(小红书/抖音/快手 全自动)+ 视频号半自动卡片("待手动发布",带打包好的内容)
4. 底部:该内容产出物列表(文案/脚本/视频/发布链接)

**内容层级:**

| 优先级 | 元素 | 说明 |
|---|---|---|
| 1 | 流水线节点状态色 | 一眼看到卡哪 |
| 2 | 失败环节的错误摘要 + 重试 | 直接可操作 |
| 3 | 各平台分发状态 | 知道哪些发成功 |

**使用的组件:** CMP-006 流水线节点、CMP-002 状态徽章、CMP-007 视频号待发布卡片、CMP-008 内容预览块

**必需状态:**
- 默认:今日尚未启动 → 空 + CTA"开始今日运营"
- 加载:节点转圈(snappy,不卡顿)
- 错误:失败节点红色 + 错误一句话 + 重试按钮
- 成功:全链路绿色 + 各平台发布链接
- 空状态:极简文案"今天还没开始,点开始今日运营"

### SCREEN-2: 账号管理(P0)

**页面目的:** 管理矩阵账号:增删改、配置主题、维护登录态。

**主要操作:** 添加账号、删除账号(AC-4)、编辑主题、重新登录

**布局结构:**
1. 顶部:添加账号按钮 + 平台筛选
2. 主内容:账号表格(紧凑行)— 平台/昵称/主题/登录态/状态/操作
3. 添加/编辑:抽屉式表单(右侧滑出),不打断列表

**必需状态:** 默认表格;加载骨架;空"还没有账号,添加第一个";错误"登录态失效,需重新登录";删除二次确认(Danger 按钮)。

(SCREEN-4/5/6 规格模式相同,密度紧凑,六态覆盖,P1 优先级,设计阶段按此模式补全)

---

## 4. 组件规格

### CMP-001: 账号卡片

**用途:** 仪表盘主体单元,一账号一卡片。**使用位置:** SCREEN-1
**变体:** 正常态 / 警告态(黄) / 失败态(红) / 运行态(蓝)
**状态:** 默认(展示) / 悬停(微亮边框) / 聚焦(边框高亮) / 点击(跳详情)
**内容规则:** 卡片紧凑(不超 200px 高),信息密度优先;状态色点固定左上角。
**a11y:** 键盘可聚焦跳转,卡片有 role=link,状态色不单靠颜色(配文字)。

### CMP-002: 状态徽章

**用途:** 全局状态语义单元。**使用位置:** 所有页
**变体:** success(绿) / warning(黄) / error(红) / info/running(蓝) / neutral(灰)
**状态:** 固定展示,无交互态
**内容规则:** 状态色点 + 简短文字(如"发布中""已发""失败"),不写长句。
**a11y:** 颜色 + 文字双编码,不单靠色。

### CMP-003: 进度条

**用途:** 任务/环节进度。**使用位置:** SCREEN-1 迷你条、SCREEN-3 节点
**变体:** 迷你(卡片内,无文字) / 标准(带百分比)
**内容规则:** snappy 填充动效(快、隐形),不脉动不闪烁。

### CMP-004: 统计数字

**用途:** 摘要条数字。**使用位置:** SCREEN-1 顶部
**内容规则:** 数字大 + 标签小 + 状态色点;数字用 tabular-nums 对齐。

### CMP-005: CTA 按钮

**用途:** 全局主操作。**使用位置:** 顶部栏
**变体:** Primary(蓝紫填充) / Secondary(描边) / Danger(红)
**状态:** 默认/悬停/聚焦/激活/禁用/加载(内联 spinner)
**内容规则:** 动词开头("开始今日运营""重试""删除"),不写名词。

### CMP-006: 流水线节点

**用途:** SCREEN-3 链路节点。**变体:** 待执行(灰)/运行中(蓝高亮)/完成(绿)/失败(红)
**内容规则:** 图标 + 环节名 + 状态 + 耗时;失败节点展开错误摘要 + 重试。

### CMP-007: 视频号待发布卡片

**用途:** 视频号半自动产出物(POC-1 决定)。**使用位置:** SCREEN-3 分发节点
**内容规则:** 打包好的标题/正文/标签/封面预览 + "复制"按钮 + "打开视频号助手"链接;极简说明"视频号需手动发布"。

### CMP-008: 内容预览块

**用途:** 文案/脚本/视频产出预览。**内容规则:** 代码块样式(深色底等宽可选),长内容折叠 + 展开。

(其余通用组件:输入框、表格行、抽屉、弹窗、Toast 等,按 Vercel 风深色紧凑默认,设计阶段补全变体七态)

---

## 5. 视觉系统

### 5.1 色彩变量

> 主锄 Vercel:深近黑底 + 克制蓝紫强调 + 四态语义色。具体 hex 标 [待补充] 交设计工具定,方向锁定。

| 变量 | 用途 | 值 / 方向 |
|---|---|---|
| color.background | 页面背景 | 近黑深灰(#0a0a0a ~ #111 量级,Vercel 风非纯黑)[待补充] |
| color.surface | 卡片、面板、弹窗 | 略亮于背景的深灰(#1a1a1a 量级)[待补充] |
| color.primary | 主操作、CTA、运行中态 | 蓝紫(Vercel 蓝紫调)[待补充] |
| color.success | 成功、已发布、正常 | 绿(饱和克制,不荧光)[待补充] |
| color.warning | 警告、登录态临期 | 黄/琥珀 [待补充] |
| color.danger | 错误、失败、删除 | 红(饱和克制)[待补充] |
| color.info | 运行中、进行中 | 蓝(可与 primary 同源)[待补充] |
| color.text.primary | 主文本 | 接近白(#ededed 量级)[待补充] |
| color.text.secondary | 次级文本、标签 | 中灰(#a1a1aa 量级)[待补充] |
| color.border | 分割线、边框 | 微亮深灰,低对比不抢眼 [待补充] |

浅色主题:深色为主,浅色为可切二等公民。浅色版本各 token 反相映射,具体 [待补充]。

### 5.2 字体排版

| 变量 | 用途 | 字号 / 字重 / 行高 |
|---|---|
| text.heading.lg | 页面标题 | [待补充,方向:18-20px / 600 / 紧凑行高] |
| text.heading.md | 区块标题 | [待补充,14-16px / 600] |
| text.body | 正文 | [待补充,13-14px / 400 / 1.5] |
| text.caption | 辅助说明、时间戳 | [待补充,12px / 400 / 次级色] |
| 数字 | 统计、进度 | tabular-nums 等宽对齐 |

字体族:**无衬线,Inter(西文)+ 思源黑体/Noto Sans SC(中文)**。等宽数字用 tabular-nums 特性,不换字体族。

### 5.3 间距系统

> 紧凑(compact)基调,4px 步进。具体值 [待补充]。

| 变量 | 用途 | 值 |
|---|---|---|
| space.xs | 紧密元素(徽章内、卡片内小间距) | [4px] |
| space.sm | 按钮/输入框内部 | [8px] |
| space.md | 卡片内边距 | [12-16px] |
| space.lg | 区块间距 | [16-24px] |
| space.xl | 大区块、页面边距 | [24-32px] |

### 5.4 形状与层级

| 变量 | 用途 | 值 / 方向 |
|---|---|---|
| radius.sm | 徽章、小组件 | [4-6px,克制不圆] |
| radius.md | 卡片、输入框 | [6-8px] |
| radius.lg | 弹窗、大容器 | [8-12px] |
| shadow.sm | 卡片轻微层级 | 极轻,深色主题阴影要克制 [待补充] |
| shadow.md | 弹窗、浮层 | 中等 [待补充] |

圆角整体克制(Vercel 风),不 pill 不锐角。

### 5.5 图标与图像风格

- 图标:线性 stroke 图标(参考 Lucide/Heroicons 风格),1.5px 描边,不填色,大小 16-20px。
- 平台图标:小红书/抖音/快手/视频号官方 logo 小尺寸(16-20px)。
- 头像:圆形,小尺寸(24-32px)。
- **无插画**(反参考已排除活泼插画);空状态/错误态用图标 + 极简文案。

---

## 6. 交互与反馈规则

### 6.1 全局交互规则

| 模式 | 规则 |
|---|---|
| 主操作 | 全局 CTA 固定顶部栏(蓝紫 Primary),"开始今日运营"始终可见 |
| 次要操作 | 描边 Secondary 按钮,就近放置 |
| 危险操作 | 删除账号/清空 = Danger 红 + 二次确认弹窗(不默认勾选) |
| 保存行为 | 账号配置自动保存 + Toast 反馈;无手动保存按钮 |
| 导航 | 侧边栏常驻,当前页高亮;面包屑仅在深层页 |
| 弹窗/抽屉 | 表单/详情用右侧抽屉,不打断列表;确认/警告用居中弹窗 |

### 6.2 反馈规则

| 场景 | UI 反馈 | 文案规则 |
|---|---|---|
| 成功 | 状态色变绿 + Toast(3s 自动消失) | 陈述事实:"已发布到抖音",不夸奖 |
| 错误 | 状态色变红 + 内联错误摘要 + 重试按钮 + Toast | 说原因:"登录态失效,请重新登录",不甩锅不道歉过度 |
| 警告 | 黄色徽章 + 内联提示 | 提示风险:"发布频率过高可能触发风控" |
| 空状态 | 图标 + 一句话 + 下一步链接 | 解释 + 引导:"还没有账号 → 去添加" |
| 加载 | 骨架屏(列表/卡片)/ 内联 spinner(按钮)/ 节点转圈(流水线) | 不写"加载中请稍候"废话,转圈即可 |

### 6.3 动效规则

- 使用动效:**是,但 snappy 隐形**(DP-003)
- 克制:动效为反馈状态存在,不吸引眼球
- 允许:状态色切换(快)、进度条填充(线性)、抽屉滑入(150-200ms)、Toast 出入
- 不允许:脉动、闪烁、弹跳、粒子、装饰性转场
- 长任务:进度条/节点状态更新,不全程转圈(避免误判卡死)
- 支持 prefers-reduced-motion:开启后所有动效降级为瞬时切换

---

## 7. UX 文案与内容规则

### 7.1 文案语气

**冷静、精准、陈述事实。** 不亲切、不说教、不夸奖、不过度道歉。像一个沉默靠谱的副驾,只说必要的话。
- 对比:"太棒了!已成功发布到抖音 ✨" ❌ → "已发布到抖音" ✅
- 对比:"抱歉,出错了呢 😢" ❌ → "登录态失效,请重新登录" ✅

### 7.2 按钮文案规则

- 主按钮:动词开头("开始今日运营""重新登录""重试")
- 次按钮:动词("取消""编辑""复制")
- 危险按钮:明确后果("删除账号"而非"删除")

### 7.3 表单文案规则

- label:名词,简洁("平台""昵称""主题")
- placeholder:具体示例("如:科技测评")
- helper text:仅必要约束("登录态有效约 7 天")
- error:说原因 + 怎么修("Cookie 已过期,重新登录获取")

### 7.4 空状态文案规则

- 解释为什么空("还没有账号")
- 给下一步("去账号管理添加")
- 不用鼓励性文案("加油!""开始你的旅程"❌)

---

## 8. 可访问性与响应式要求

### 8.1 可访问性目标

基础可访问性,WCAG 2.2 AA 对比度(深色主题文字对比度尤其注意)。

### 8.2 必需可访问性规则

- [ ] 所有可交互元素键盘可达(Tab 顺序合理)
- [ ] 焦点状态清晰可见(边框高亮,深色主题焦点要够亮)
- [ ] 表单字段有 label 关联
- [ ] 错误信息与字段关联(aria-describedby)
- [ ] **不只靠颜色表达状态**(状态色 + 文字/图标双编码,DP-002 补强)
- [ ] 正文与背景对比度 ≥ 4.5:1
- [ ] 图标有 alt 或标装饰性
- [ ] 页面/区块标题结构清晰(语义化 h1-h3)

### 8.3 响应式规则

> 桌面优先(用户主要在电脑上运营);移动端为次要场景(可能手机盯进度),适配但不优化。

| 断点 / 设备 | 规则 |
|---|---|
| 桌面(≥1024px) | 主战场。侧边栏常驻,仪表盘 3-4 列网格,全功能 |
| 平板(768-1023px) | 侧边栏可收起,仪表盘 2 列,功能不减 |
| 移动(<768px) | 侧边栏变抽屉,仪表盘单列堆叠,只保留看状态+触发 CTA 核心功能,复杂配置建议桌面操作 |

移动端不追求全功能等价,只保"看状态 + 点 CTA + 看异常",配置类操作引导回桌面。

---

## A. 终端与对话式 Agent 形态规格

**N/A。** 本产品为界面型 Web 面板,不含终端型或对话式 Agent 形态。

---

## 9. 实现说明

- 前端栈:[待补充,建议 React + Tailwind + shadcn/ui(天然契合 Vercel 风,深色紧凑开箱即用)]
- 组件库:无现成,建议基于 shadcn/ui 二次定制
- 暗色模式:**深色为一等公民(默认),浅色可切**(用户已定)
- 品牌资产:无现成 logo/VI,从零选,蓝紫强调色为基调
- 必用设计模式:卡片网格、横向流水线、侧边栏导航、状态色双编码
- 避免:大面积留白、活泼插画、多色渐变、装饰动效、纯黑绿终端感(反参考)
- 混剪链路依赖 MoneyPrinterTurbo 架构(详见 Product-Spec FLOW-3),流水线节点需支持 6 子环节(拆段/找素材/TTS/字幕/拼接/后处理)进度可视化

---

## 10. 假设与待确认问题

### 10.1 假设

| 编号 | 假设 | 依据 | 错误风险 |
|---|---|---|---|
| DASM-001 | 紧凑密度适合目标用户(运营者长时间盯盘) | 用户选 compact,运营面板标配 | 若用户实际觉得太挤,密度可调,低风险 |
| DASM-002 | Vercel 深蓝风契合"省心感" | 用户选 Vercel 主锄 + 专业现代深蓝人格 | 若实际做出来偏冷不够"省心",靠文案/状态色暖化补救 |
| DASM-003 | 移动端次要,只看状态不需全功能 | 用户主战场桌面,运营配置在电脑 | 若用户重度手机用,移动端需补功能,中风险 |

### 10.2 待确认问题

| 编号 | 问题 | 阻塞 | 备注 |
|---|---|---|---|
| DQ-001 | 所有 token 具体 hex 值 | No | 交设计工具/设计稿阶段定 |
| DQ-002 | 前端技术栈确认(React? Vue?) | No | DEV-PLAN 阶段定 |
| DQ-003 | 是否需要 favicon/产品名 | No | 发布前补 |
| DQ-004 | 视频号待发布卡片的具体打包内容字段 | No | 依视频号助手实际表单字段定,开发阶段核 |

---

*本文为单一视觉真相源。任何视觉方向变更先改这里 + 记录,再动设计稿/代码。与 Product-Spec.md 的 FLOW/SCREEN/CMP 编号保持一致。*
