# 废才框架 · Codex 版 使用说明

这是一套给 Codex 使用的产品开发工作流。它不是单纯提示词，也不是一堆越写越长的规则，而是四样东西配合起来工作：

- `AGENTS.md`：主控文件，负责角色、路由、总规则和工作边界。
- `.agents/skills/`：Skill 能力模块，负责不同阶段的专业标准。
- `.codex/hooks/`：确定性门禁脚本，负责那些不该靠模型反复记住的硬判断。
- `.codex/agents/`：固定 Sub-Agent 定义，目前只有代码审查和自进化消化两个。

当前版本的重点是“轻重分流”。以前的问题是，你只是想改一个按钮、修一行样式，Agent 也可能把你拖进需求文档、开发计划、两阶段审查、commit、push 这一整套重流程。现在改成三档：小修走快修，常规功能走适中流程，从 0 到发布才走严格流程。

这版不是把严格流程砍掉，而是让它只在该出现的时候出现。

## 安装

把这个包放进你的项目根目录，最后应该长这样：

```text
project/
├── AGENTS.md
├── .agents/
│   └── skills/
└── .codex/
    ├── hooks.json
    ├── hooks/
    ├── agents/
    ├── evolution/
    └── workflow-mode
```

具体步骤：

1. 把 `AGENTS.md` 放进项目根目录。
2. 把本包里的 `agents` 文件夹重命名为 `.agents`，放进项目根目录。
3. 把本包里的 `codex` 文件夹重命名为 `.codex`，放进项目根目录。
4. 重命名后 `.agents` 和 `.codex` 会变成隐藏文件夹，这是正常的。
5. 在项目根目录启动 Codex。

Windows PowerShell 里可以这样检查：

```powershell
Get-ChildItem -Force
```

你应该能看到：

```text
AGENTS.md
.agents
.codex
```

macOS Finder 里看不到隐藏目录时，按 `Cmd+Shift+.` 显示隐藏文件。

## 第一次启动会发生什么

Codex 进入项目后，先读 `AGENTS.md`。它会知道自己不是普通聊天助手，而是一个产品经理兼全栈开发教练：先判断你要做的事情属于哪个级别，再决定调用哪个 Skill。

如果 `.codex/evolution/signals.jsonl` 里有用户纠正信号，启动时会提示有待消化内容。这个机制是自进化用的，不是每次都跑重任务。没有信号时，它基本无感。

默认工作模式写在：

```text
.codex/workflow-mode
```

初始内容是：

```text
fast
```

也就是默认日常快修，不默认进入严格发布流程。

## 三档工作模式

这一版最重要的变化就是三档模式：`Fast Patch`、`Normal Feature`、`Strict Release`。

### Agent 先判档，不让你先做选择题

每轮产品、代码、修复、审查或发布请求，都会先走 `workflow-router`。它只做一件事：判断这次任务该按哪档处理。

它不会默认问你：

```text
你要 Fast Patch、Normal Feature 还是 Strict Release？
```

这种问题是把判断责任甩给你，没意义。合理做法是 Agent 先判断，再告诉你：

```text
我按 Fast Patch 快修处理，因为这是单文件文案修改。
```

或者：

```text
我建议按 Normal Feature 处理，因为它会影响设置页交互和导出逻辑。按这个继续吗？
```

只有边界不清、会明显增加耗时、涉及上线成本、数据或安全风险时，才问你确认。

### Fast Patch

Fast Patch 是日常小修模式。适合这些情况：

- 改文案。
- 改颜色、间距、字号这类小样式。
- 修一个明显的小 bug。
- 调整单个组件的局部行为。
- 单文件或少量文件的低风险改动。
- 不改变产品需求。
- 不改数据库。
- 不碰权限、支付、密钥、发布。

Fast Patch 会交给 `fast-patch` Skill。它的原则是“小修小做”：

- 只读必要文件，不扫完整产品文档。
- 不更新 `Product-Spec.md`。
- 不更新 `DEV-PLAN.md`。
- 不默认派 `code-reviewer`。
- 不自动 commit。
- 不自动 push。
- 只跑最小必要验证。

举例：

```text
把首页按钮文案从“提交”改成“保存”
```

这应该是 Fast Patch。Agent 应该直接定位按钮、修改文案、做最小验证，然后汇报。它不该把你拖进需求访谈。

再比如：

```text
这个弹窗标题太挤了，间距调大一点
```

这也是 Fast Patch。它应该看相关组件和样式，不该重写设计规范。

### Normal Feature

Normal Feature 是常规功能模式。适合这些情况：

- 新增用户可感知功能。
- 修改交互流程。
- 跨多个文件。
- 影响 API、状态结构或页面行为。
- 可能需要少量同步产品文档或开发计划。
- 不涉及上线、支付、数据库迁移、安全敏感内容。

举例：

```text
给设置页加一个导出配置功能
```

这不是小修，因为它会新增行为、状态和可能的文件下载逻辑。但它也不一定需要严格发布流程。合理处理是 Normal Feature：必要时补充 Spec 或 DEV-PLAN，直接实现，跑相关测试。如果只是本地功能，不默认派 `code-reviewer`。

Normal Feature 的特点是“够用但不装腔”：

- 该读文档就读。
- 该更新文档才更新。
- 该测试就测试。
- 高风险、跨模块或你要求审查时，才派 `code-reviewer`。

### Strict Release

Strict Release 是严格发布模式。适合这些情况：

- 从 0 到 1 做一个完整产品。
- 明确说要做到可发布。
- 上线、部署、打包。
- 认证、权限、支付。
- 数据库迁移。
- 安全敏感改动。
- 涉及密钥、生产数据、用户隐私。
- 你明确说“严格模式”。

Strict Release 会保留完整流水线：

```text
Product-Spec
→ Design-Brief 可选
→ DEV-PLAN
→ dev-builder
→ code-reviewer
→ 测试完整性
→ 编译验证
→ 功能测试
→ release-builder
```

这就是从 0 到发布该有的严谨度。它慢，但慢得有理由。

手动进入 Strict Release：

```powershell
Set-Content -NoNewline .codex\workflow-mode strict
```

或者在 bash 里：

```bash
echo strict > .codex/workflow-mode
```

回到 Fast Patch：

```powershell
Set-Content -NoNewline .codex\workflow-mode fast
```

或者：

```bash
echo fast > .codex/workflow-mode
```

Strict Release 下如果确实要 commit 后自动 push，需要显式允许：

```bash
touch .codex/allow-auto-push
```

不创建这个文件，就算 strict 模式下 commit 了，也不会自动 push。

## 十三个 Skill：前馈引导，不是流程枷锁

Skill 的作用是在 Agent 动手前注入标准。它不是让模型机械照剧本走，而是告诉模型：这类任务的目标是什么、边界是什么、验收标准是什么。

当前一共 13 个 Skill。

### workflow-router

这是入口 Skill。每轮产品、代码、修复、审查或发布请求，先用它判档。

它只做分类，不写代码。它会判断这次任务属于：

- `Fast Patch`
- `Normal Feature`
- `Strict Release`

然后写入 `.codex/workflow-mode`。这一步很关键，因为后面的 hook 和 Skill 都会参考这个模式。

它的价值是防止两种错：

- 小问题被拖进重流程。
- 高风险任务被当小修糊弄过去。

比如你说：

```text
这个卡片标题换个词
```

它应该判为 Fast Patch。

你说：

```text
加一个团队成员邀请功能
```

它应该判为 Normal Feature。

你说：

```text
把这个产品从零做到能上线
```

它应该判为 Strict Release。

### fast-patch

这是日常效率的关键。它专门处理小修、小 bug、文案、样式、局部 UI、单文件局部改动。

它有几个硬边界：

- 不更新产品文档。
- 不更新开发计划。
- 不派子智能体审查。
- 不自动 commit。
- 不自动 push。
- 不顺手重构。

如果它修着修着发现事情变大了，比如一个按钮文案背后其实牵扯权限状态、后端 API 或数据结构，它会停止 Fast Patch，把任务交回 `workflow-router` 升级判档。

这就是快修模式不失控的关键：能快就快，不能快就承认不能快。

### product-spec-builder

负责需求收集和需求变更。

它不该被小修触发。只有你在表达产品想法、新功能、需求变化时，它才应该出来。

它的价值在源头。产品开发里最贵的浪费不是代码写慢，而是需求一开始就模糊。这个 Skill 会追问用户、用户场景、范围、非目标、AI 能力边界、验收标准，把“我想做个东西”变成能开发的 `Product-Spec.md`。

比如你说：

```text
我想做一个视频剪辑 Agent
```

它不会直接让 dev-builder 开工，而是先问清楚：服务谁、解决哪个具体剪辑痛点、第一版不要什么、AI 到底负责判断还是只负责生成建议。

但你说：

```text
把按钮颜色调浅一点
```

它不该出现。

### design-brief-builder

负责设计规范。

它不只是问“你喜欢什么风格”。它会先判断产品形态：这是 SaaS、桌面工具、对话式 Agent、终端工具，还是内容型网站。形态错了，后面所有视觉问题都会错。

它会把抽象词翻成具体设计约束。比如“高级感”不能直接写进设计规范，它要落成：留白、密度、字体、颜色、阴影、交互节奏、组件圆角、信息层级。

它是可选环节。并不是所有项目都必须先写设计规范。但如果你要做完整产品，尤其是从 0 到发布，它很有价值。

### design-maker

负责设计图制作。

它适合在有 Figma、Pencil 或类似设计工具连接时使用。它的任务不是随便画一张好看的图，而是根据 `Product-Spec.md` 和 `Design-Brief.md` 把页面、组件、状态补齐。

它要关注三件事：

- Spec 里每个有 UI 的功能，都应该有对应页面或状态。
- 页面不只有默认态，还要有空状态、加载态、错误态。
- 先做可复用组件，再组合页面。

没有设计工具时，它可以不走。开发可以继承现有项目页面和组件先例。

### dev-planner

负责开发计划。

它读 `Product-Spec.md`，必要时带上 `Design-Brief.md` 和设计稿，然后拆成 `DEV-PLAN.md`。

它关心依赖顺序，而不是写漂亮计划。基础设施要先做，被依赖的模块要先做，风险高的东西要尽量前置验证。

它有两个硬标准：

- 每个 Phase 完成后必须能编译、能运行、能看到效果。
- 计划里不能出现 TBD、待补充、“类似上面”这类空话。

Normal Feature 里，只有当功能变化影响阶段、文件清单或交付边界时，才需要回到 dev-planner。Fast Patch 不用。

### dev-builder

负责项目开发。

过去所有开发都容易被它拖进完整 review 闭环。现在它按工作模式执行：

- Fast Patch：优先不进 dev-builder，交给 `fast-patch`。
- Normal Feature：按相关 Phase 或 Task 实现，跑相关验证，按风险决定是否审查。
- Strict Release：走完整 per-task review 和四步验证。

它的底层纪律仍然保留：

- 改代码前先评估影响范围。
- 删除或修改被多处引用的东西前，先搜引用点。
- 优先复用现有组件和样式。
- 按真实数据和真实行为做功能，不写装饰性假功能。
- 完成声明要带验证证据。

这就是“严格纪律”和“日常速度”的平衡：纪律保留，触发条件变准。

### bug-fixer

负责修 bug。

它不是“看到报错就乱改”。它的流程是：

1. 收集症状和报错。
2. 找复现步骤。
3. 看最近变化。
4. 提出最多三个假设。
5. 一次验证一个。
6. 一次只修一个逻辑点。
7. 修完回归。

如果连续几次修不好，它应该停下重审，而不是继续叠补丁。

Fast Patch 的小 bug 可以直接走 `fast-patch`。当 bug 需要系统性定位、涉及多组件或根因不清时，再走 `bug-fixer`。

### code-review

负责代码审查。

它分两阶段：

- Stage 1：做对了没有。对照 Spec、设计稿、功能要求，查完整性。
- Stage 2：做好了没有。看质量、安全、测试真实性、潜在漂移。

当前触发规则是：

- Fast Patch：不默认触发。
- Normal Feature：高风险、跨模块或用户要求时触发。
- Strict Release：默认触发。

触发后由 `code-reviewer` Sub-Agent 执行。它只输出报告，不直接修复。主 Agent 拿报告决定回 dev-builder 还是 bug-fixer。

### release-builder

负责构建、打包、部署、发布。

它的核心原则是：dev 环境测通不等于发布产物能用。打包后的路径、权限、依赖、环境变量都可能变。

所以它关注：

- 构建是否通过。
- 发布产物是否能实际运行。
- 是否泄露本地路径、数据库、环境变量、API Key、密码。
- Desktop、Web、CLI 不同发布形态的验证方式。

只有用户明确要打包、部署、上线、交付时才用它。

### goal-creator

负责生成自驱目标指令。

当你想把一个完整阶段交给 Codex 自己跑，比如“完成 DEV-PLAN 里的 Phase 2”，不要直接说“你自己弄完”。这种目标太虚。

`goal-creator` 会把目标改写成可验证指令：

- 要产出哪些文件。
- 要跑哪些命令。
- 通过标准是什么。
- 哪些边界不能碰。
- 什么情况下必须停下来找用户。

目标驱动执行的关键不是“让 Agent 更自由”，而是把完成条件写到它无法糊弄。

### evolution-engine

负责自进化。

当你纠正 Agent，比如“不是这个意思”“你又把小修改成大流程了”，hook 会把明显纠正信号写进 `.codex/evolution/signals.jsonl`。之后 `evolution-engine` 消化这些信号，提炼成通用规则改进建议。

它只提议，不替用户决定。建议会写到 `.codex/evolution/proposals.md`，由主 Agent 问你同不同意。

这个机制用来修规则，不用来替代当前任务本身。

### skill-builder

负责创建新 Skill。

这是最后手段，不是普通开发步骤。只有当现有 Skill 无法表达一种稳定、重复出现的新能力，或者 evolution 建议新建 Skill 且你确认时，才用它。

这次新增的 `workflow-router` 和 `fast-patch` 就是这种情况：它们不是项目功能，而是工作流能力本身，需要独立成 Skill。

## 规划与执行：通用组织方式

这套框架不是每个 Skill 都写一大坨步骤，而是让主 Agent 共享一条通用执行模式。

任何环节开工前，主 Agent 都要先把任务拆成有序、可独立验收的步骤。每步写清楚目标和完成标准。

执行方式按隔离程度选择：

- 步骤少、互相耦合：主 Agent 自己做。
- 步骤多、互不依赖：主 Agent 可以并行查资料、读文件、跑验证。
- 需要独立判断或干净上下文：显式 spawn 子 Agent。

但子智能体不是越多越高级。默认主 Agent 自己做。只有隔离收益大于上下文复制成本时，才派子 Agent。

执行时守三条：

- 上下文自带：开工前读相关原文，不靠记忆。
- 结果自检：用完成标准和证据说话。
- 排障自驱：没达标就定位、修复、重验，同一问题反复卡住才找用户。

## Sub-Agent 调度

固定 Sub-Agent 只有两个。

### code-reviewer

代码审查员。只在触发 code-review 时使用。

它做两阶段审查：

- Stage 1：Spec Compliance，检查功能和设计是否实现。
- Stage 2：Code Quality，检查质量、安全、测试、漂移。

它不修代码，只交报告。

### evolution-runner

进化消化器。只处理 evolution 信号。

它读取：

```text
.codex/evolution/signals.jsonl
.codex/evolution/proposals.md
```

它把用户纠正和重复失败模式整理成规则建议，但不自动落地。

## Hooks：确定性门禁

Hooks 负责确定性判断。它们不是拿来替代 Agent 思考的，而是负责那些“该不该拦”很明确的事情。

当前 `.codex/hooks.json` 保留这些事件：

### UserPromptSubmit

运行：

```text
.codex/hooks/detect-feedback-signal.sh
```

作用：检测明显纠正语句，比如“不是这样”“你理解错了”“没必要”“每次都”。命中时写入 evolution 队列。

它只入队，不当场消化，避免阻塞当前请求。

### SessionStart

运行：

```text
.codex/hooks/check-evolution.sh
```

作用：启动时提示是否有待处理 evolution 信号或建议。

如果没有信号，基本无感。

### PostToolUse shell

运行：

```text
.codex/hooks/auto-push.sh
```

作用：commit 后自动 push 的入口。但现在非常克制：

- 必须是 `strict` 模式。
- 必须存在 `.codex/allow-auto-push`，或环境变量 `ALLOW_AUTO_PUSH=1`。
- 当前分支不能是 `main` 或 `master`。

否则不会 push。

### PostToolUse edit/write

运行：

```text
.codex/hooks/mark-review-needed.sh
```

作用：代码文件被编辑后标记需要 review。但只有 `strict` 模式才生效。

Fast Patch 和 Normal Feature 不会因为编辑文件就被强制 review。

### Stop

运行：

```text
.codex/hooks/stop-gate.sh
```

作用：停止前检查 `.codex/.needs-review`。只有 strict 模式下，代码改了但没有 review 才阻塞。

这解决了之前最烦人的问题：小修不会因为没派 `code-reviewer` 就收不了尾。

### PreToolUse

当前已关闭。

旧版每次 shell 调用前都会跑脚本，在 Windows/WSL 环境里单次启动就可能接近一秒，小任务里会被放大。现在把这层关掉，日常速度会明显好一点。

## 自进化机制

自进化不是后台偷偷改规则，而是“收集信号 → 生成建议 → 问你要不要改”。

流程是：

1. 你纠正 Agent。
2. `detect-feedback-signal.sh` 把明显纠正写入 `signals.jsonl`。
3. SessionStart 时提示有信号。
4. 主 Agent 显式派 `evolution-runner` 消化。
5. 建议写进 `proposals.md`。
6. 主 Agent 逐条问你同不同意。
7. 你同意才改 `AGENTS.md`、某个 Skill 或 hook。

适合沉淀的规则必须是通用失败模式。项目专属偏好不要硬塞进通用规则，不然这套框架会越用越肿。

## 常用场景

### 改一个小问题

你说：

```text
把保存按钮文案改成“保存到本地”
```

预期：

- `workflow-router` 判为 Fast Patch。
- `fast-patch` 定位相关文件。
- 修改文案。
- 做最小验证。
- 不触发 code-reviewer。
- 不自动 commit/push。

### 修一个小 bug

你说：

```text
设置页点取消没反应，帮我修一下
```

如果是单组件事件绑定问题，走 Fast Patch。

如果查到背后牵扯状态管理、路由、后端 API，`fast-patch` 会升级回 `workflow-router`，改按 Normal Feature 或 bug-fixer 处理。

### 加一个普通功能

你说：

```text
给设置页加一个导出配置功能
```

预期：

- `workflow-router` 判为 Normal Feature。
- Agent 简短说明理由。
- 必要时同步 Spec 或 DEV-PLAN。
- 实现功能。
- 跑相关验证。
- 不默认派 code-reviewer，除非风险较高或你要求。

### 从 0 做到发布

你说：

```text
从 0 开始做一个桌面端视频剪辑 Agent，并按严格流程做到可发布
```

预期：

- `workflow-router` 判为 Strict Release。
- `product-spec-builder` 先收需求。
- 可选 `design-brief-builder` 和 `design-maker`。
- `dev-planner` 拆计划。
- `dev-builder` 实现。
- `code-reviewer` 审查。
- `release-builder` 打包或部署。

这时慢是正常的，因为你要的是从想法到发布产物，不是改一个小问题。

### 用户明确要求审查

你说：

```text
这次改完帮我完整 code review
```

即使不是 Strict Release，也会触发 `code-review`，并派 `code-reviewer`。

### 用户明确不想重流程

你说：

```text
只是快修，不要改文档，不要 review
```

如果任务不涉及生产、支付、数据库、安全或发布，Agent 应该按 Fast Patch 做。

如果任务看起来高风险，Agent 必须提醒你升级风险，而不是盲目听 fast。

## 常用命令

查看当前模式：

```powershell
Get-Content .codex\workflow-mode
```

切到 Fast Patch：

```powershell
Set-Content -NoNewline .codex\workflow-mode fast
```

切到 Normal Feature：

```powershell
Set-Content -NoNewline .codex\workflow-mode normal
```

切到 Strict Release：

```powershell
Set-Content -NoNewline .codex\workflow-mode strict
```

允许 strict 下 commit 后自动 push：

```powershell
New-Item -ItemType File .codex\allow-auto-push -Force
```

关闭自动 push 允许：

```powershell
Remove-Item .codex\allow-auto-push
```

清掉 strict review 标记：

```powershell
Set-Content -NoNewline .codex\.needs-review clean
```

一般不建议手动清 `.needs-review`。如果 strict 模式下被 stop-gate 拦住，正常做法是跑完 code-review 后再写 clean。

## 维护原则

这套框架后续要继续瘦，而不是继续胀。

维护时按这个归属放规则：

- 判档细则放 `workflow-router/SKILL.md`。
- 快修细则放 `fast-patch/SKILL.md`。
- 需求访谈放 `product-spec-builder/SKILL.md`。
- 开发纪律放 `dev-builder/SKILL.md`。
- 审查标准放 `code-review/SKILL.md`。
- 发布标准放 `release-builder/SKILL.md`。
- 确定性门禁放 `.codex/hooks/`。
- 总路由和边界放 `AGENTS.md`。

不要把所有东西都塞回 `AGENTS.md`。`AGENTS.md` 一旦变成巨型说明书，Agent 每轮都要背着跑，速度和判断都会变差。

好的规则应该更短、更准、更少误触发。不是越详细越高级，触发得准才高级。
