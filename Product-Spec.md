# Product-Spec · 自媒体全自动运营流水线

> 给一人公司用的自媒体全自动流水线——从热点到发布到互动,一键跑完,管多个号多个平台。
>
> 单一真相源。后续 Design Brief、DEV-PLAN、代码均以此为据。任何变更先改本文件再动代码。

---

## 0. 元信息

| 项 | 值 |
|---|---|
| 产品代号 | 待定(建议 AutoMedia-Pipeline) |
| 版本 | v1.0(初版 Spec) |
| 日期 | 2026-07-01 |
| 模式 | Normal Feature(单人开发,聚焦功能落地) |
| 产品类型 | AI 自媒体运营工具(非 agent 自主系统,故不进 Phase 8) |

---

## 1. 问题与人

### 1.1 真实问题
一人公司运营者,一人扛自媒体全链条:**查热点 → 写文案 → 出视频脚本 → 剪视频 → 分发 → 回评论 → 看数据**。六件事压一个人身上,件件都要做,件件都烦,时间永远不够。

### 1.2 唯一主用户
**一个人运营多平台多账号矩阵的内容创业者**(就是用户本人)。具体画像:
- 一个人或极小团队(1-3 人)
- 在小红书、抖音、快手、视频号 4 个平台各有 1-3 个号(矩阵 ≤10 个号)
- 不同号做不同主题(多 IP 主题并存)
- 全栈运营:既想创意、又写文案、又剪视频、又发布、又互动

### 1.3 现状 workaround
靠手动 + 若干零散工具凑合:
- 热点:刷各平台热榜,人工判断哪些值得做
- 文案:手写或 ChatGPT 单次对话,一篇篇产
- 视频:自己剪,剪一个 1-2 小时
- 分发:手动逐个平台登录、复制粘贴、上传,4 个平台发一条要 20-30 分钟
- 回评论:挨个平台翻、挨条回,容易漏、不及时
- 数据:各平台后台挨个看,记在脑子里,没有统一视图

### 1.4 痛的代价
- **时间**:上述全链条手工,一条内容从选题到发布到互动,吃掉半天到一天
- **规模上限**:一个人最多管 5-10 个号,再多个不动
- **机会成本**:手工分发/回评论占掉的时间,本该用来想更好的内容
- **失败风险**:漏回评论=掉粉;发布不及时=错过流量窗口

---

## 2. Job 与成功

### 2.1 Job 陈述(不含任何方案)
"帮我把一条选题,从热点到 4 个平台发布到评论互动,全自动跑完,我只需要看结果。"

### 2.2 成功度量(可度量、不可灌水)
> ⚠️ 原版本"30 分钟"为编造,无基线数据支撑。POC-3 实测后重定。见 7.5 待确认 Q5。

- **北极星**:全链路人工干预时间,目标比手工流程缩短 ≥70%(基线由用户填 POC-3 测量表得出,见 7.7)
- **辅助指标**(待基线确定后填具体数字):
  - 单条内容从选题到 3 平台全自动发布:端到端耗时 [基线 × 30%]
  - 日均可处理内容数:[基线 × 3]
  - 评论响应中位时间:从现状小时级/天级降到 ≤10 分钟
- **验收基线**:连续运行 7 天,3 平台矩阵号每日有内容产出,创作+分发环节无人工介入

### 2.3 维生素还是止痛药
**止痛药。** 时间痛是真实的、高频的、正在发生的。用户已经在为此手动干活、付时间成本。

---

## 3. 范围与非目标

### 3.1 v1 必做(P0)
| ID | 范围 | 优先级 |
|---|---|---|
| SCOPE-1 | **3 平台全自动(小红书/抖音/快手) + 视频号半自动**(POC-1 结论,见 7.5 假设 A-5) | P0 |
| SCOPE-2 | 全链路 6 环节(热点/文案/脚本/剪辑/分发/回评) | P0 |
| SCOPE-3 | 多账号矩阵管理(≤10 个号,每号独立主题) | P0 |
| SCOPE-4 | 全自动执行(用户接受封号风险,见假设 A-1) | P0 |
| SCOPE-5 | Web 面板可视化(账号状态、任务进度、触发操作) | P0 |

### 3.2 非目标(明确不做)
| ID | 不做的事 | 理由 |
|---|---|---|
| NON-1 | 团队协作/多用户 | 一人公司,单人场景 |
| NON-2 | 对外 SaaS / 多租户 | 先自己用,未来再说 |
| NON-3 | ontology / 知识图谱推理 | 已确认放下,与本需求无关 |
| NON-4 | 视频真人出镜/AI 数字人生成 | 混剪为主,不做数字人 |
| NON-5 | 内容深度原创策划 | AI 生成的是"工业化量产内容",不替代真人创意判断。边界:AI 产出初稿,用户可改,属"AI 辅助"非"全自动原创" |
| NON-6 | 商业化变现/带货/电商集成 | 运营环节,不在本产品职责内 |
| NON-7 | 数据深度分析与 BI 报表 | **边界**:v1 只做基础数据回显(发布数/点赞/评论/转发等原始指标),不做趋势分析/归因/预测等 BI 能力 |

### 3.3 v1 单一必须功能
**"一键全链路":输入一个选题或让它自动选热点,30 分钟内 4 个平台 10 个号矩阵内容自动发出去,评论自动回。**

---

## 4. 任务、旅程与功能

### 4.1 用户任务(TASK)
| ID | 任务 | 频率 |
|---|---|---|
| TASK-1 | 配置账号矩阵(添加平台账号、设主题) | 一次性 + 偶尔改 |
| TASK-2 | 一键启动全链路(选热点 or 输选题) | 每日多次 |
| TASK-3 | 监控运行进度(Web 面板看状态) | 每日 |
| TASK-4 | 人工干预异常(失败、被风控、需改文案) | 按需 |
| TASK-5 | 查看产出与基础数据 | 每日/每周 |

### 4.2 端到端用户旅程
1. 用户打开 Web 面板 → 看到 10 个号的状态总览
2. 点"开始今日运营"→ 系统自动抓 4 平台热榜,按各号主题筛选题
3. 用户扫一眼选题列表,点确认(或全自动跳过确认)→ AI 按各号主题生成文案+脚本
4. 系统自动混剪视频(AI 找素材为主,留手动上传接口)
5. 系统自动分发到 3 平台(小红书/抖音/快手)×N 账号;**视频号内容由 AI 备好打包,用户手动发布(POC-1 结论)**
6. 系统自动监测并回复评论(仅 3 全自动平台;视频号评论人工处理)
7. 用户在面板看结果 + 基础数据,异常告警

### 4.3 功能清单与行为

#### FLOW-1 · 热点采集与选题
- **触发**:TASK-2 启动,或定时任务
- **系统行为**:调 MediaCrawler 爬 4 平台热榜 → 按各账号配置的主题过滤排序 → 输出选题候选
- **产出**:选题列表(含平台来源、热度、与账号主题匹配度)
- **MUST**:主题过滤必须可配置;支持排除词
- **输入校验**:主题关键词非空;平台开关至少开 1 个

#### FLOW-2 · 文案与视频脚本生成
- **触发**:FLOW-1 选题确认后
- **系统行为**:按账号主题+平台调性,调豆包/通义千问 API 生成文案+视频脚本
- **产出**:每账号每平台的文案(标题/正文/标签)+ 视频脚本(分镜/口播)
- **MUST**:不同平台文案长度/风格自动适配(小红书长文标签多、抖音短平快等)
- **SHOULD**:支持人工微调后再进下一环
- **输入校验**:选题非空;账号主题已配置

#### FLOW-3 · agent 智能剪辑(用现成框架 + 中转 LLM API,终版 v1.5)
- **触发**:FLOW-2 文案产出后,或用户手动上传长视频时
- **设计哲学**:**GPT-5.5/Claude 当大脑(中转 API)+ HyperFrames/Skills 现成框架当手脚 = agent 当剪辑师。不调黑箱成片 API,不自建五层架构。** 参考 B 站《Codex 剪视频》教程路线,已被验证可行。
- **两个场景,同一个 agent 引擎:**

| 场景 | 输入 | agent 怎么干 | 输出 |
|---|---|---|---|
| **A. 高光提取** | 用户上传的长视频 | FFmpeg 抽帧 → LLM 看图理解内容/找高光时刻 → LLM 出剪辑决策(哪几秒是精华)→ FFmpeg 按决策剪切拼接 | 60s 高光短剪辑 |
| **B. 从零生成** | FLOW-2 文案脚本 | LLM 出分镜/剪辑方案 → Pexels 找素材 + TTS 配音 + Whisper 字幕 → Remotion/HyperFrames 渲染成片 | 全新拼装视频 |

- **agent 引擎核心(两场景共用)**:

| 组件 | 作用 | 技术 |
|---|---|---|
| **大脑(LLM)** | 理解内容、做剪辑决策、出方案 | **中转 API:智谱 GLM / DeepSeek / 通义 / 豆包**(用户指定,文本+多模态视觉看抽帧) |
| **眼睛(看视频)** | 把视频变成模型能看的图片 | FFmpeg 抽帧(按 1 帧/秒或关键帧) |
| **手脚(执行)** | 按决策执行剪切/拼接/渲染 | **Remotion(写 HTML 渲染成片)或 FFmpeg(直接剪切拼接)**;Whisper 字幕;TTS 配音 |
| **技能包(Skills)** | 标准化剪辑指令集(转场/字幕/卡点) | 现成开源 [chengfeng-videocut-skills](https://youmind.com/zh-CN/landing/x-viral-articles/codex-ai-video-editing-workflow) 或 HyperFrames |

- **工作流(简化版)**:
  ```
  输入(长视频/文案脚本)
    ↓
  LLM 理解 + 出剪辑决策(结构化:切点时间码/分镜/转场/BGM)
    ↓
  调工具执行(FFmpeg 剪切 / Remotion 渲染 / TTS / Whisper)
    ↓
  成片 → 可选人工预览/打回重剪
    ↓
  进 Content 表
  ```

- **产出**:成片 MP4(9:16 竖屏,每账号每平台适配)
- **MUST**:
  - LLM 必须能"看"视频(通过抽帧),不是盲剪——这是区别于黑箱 API 的核心
  - 两场景产出统一进 Content 表,后续 FLOW-4 不区分来源
  - 混合素材(场景 B):Pexels 找为主,手动上传兜底
  - 失败可重试,保留中间产物(剪辑决策 JSON、抽帧)
  - 支持全自动(无人值守)+ 人机协同(出方案后人工可改)
- **成本约束**:
  - LLM 调用:抽帧数量决定成本(1 帧/秒 × 视频时长 × 单价);用中转 API 成本可控
  - 渲染:Remotion 是 HTML 渲染(比纯 FFmpeg 高效但仍吃 CPU);FFmpeg 直接剪切轻量
  - 单机吞吐:抽帧看图是 IO(可并发),渲染是 CPU(需限流排队)
- **已知约束**:
  - LLM 看抽帧是"近似看视频",非逐帧连续理解——对节奏/连续动作类判断有局限,适合口播/讲解/图文类,不适合舞蹈/体育等强节奏
  - 中转 API 的多模态视觉能力需 POC 验证(智谱 GLM-4V / 通义千问 VL 等国内模型看图够不够用)
- **不做什么**:
  - 不自建视频理解模型(用 LLM 抽帧方案)
  - 不调黑箱成片 API(agent 自主决策,非厂商替你决定)
  - 不做洗稿搬运(只举一反三,不碰别人素材)

#### FLOW-4 · 分发(分两档:全自动 + 半自动)
- **触发**:FLOW-3 成片后
- **系统行为**:
  - **全自动档**:调 Social-Auto-Upload / Spreado 底层,Playwright 模拟登录,发布到小红书/抖音/快手
  - **视频号半自动档**:AI 把标题/正文/标签/封面打包,在面板生成"待发布"卡片,用户手动登录视频号助手发布
- **产出**:各平台发布结果(成功/失败/链接);视频号产出"待手动发布"卡片
- **MUST**:复用 Chrome CDP 登录态降风控;失败可重试
- **风险**:见假设 A-1,用户已认封号风险;视频号因政策高风险不纳入全自动(假设 A-5)

#### FLOW-5 · 自动回评论
- **触发**:发布后定时轮询各平台新评论(仅 3 全自动平台)
- **系统行为**:调 MediaCrawler 抓评论 → AI 生成回复 → Playwright 模拟回复
- **产出**:评论回复记录
- **MUST**:回复频率限速防风控(同账号同平台回复间隔 ≥X 秒,X 待实跑调试)
- **护栏**:回复内容人工抽检(见 5.3),不达标调优 prompt

#### FLOW-6 · 多账号矩阵管理
- **触发**:TASK-1 配置时
- **系统行为**:CRUD 账号(平台/账号名/登录态/主题/人设/发布计划)
- **产出**:账号配置持久化
- **MUST**:登录态(cookie)加密存储;每号独立主题;可禁用某号
- **MUST**(可建必可删):账号可删除,删除时清理其所有任务/数据

#### FLOW-7 · Web 面板
- **触发**:用户打开面板
- **系统行为**:展示账号总览、任务进度、产出列表、基础数据、异常告警
- **产出**:可视化界面
- **MUST**:实时任务状态(进行中/成功/失败);异常一键查看日志

#### FLOW-8 · 任务编排与调度(核心架构,评审指出原 Spec 缺失)
- **触发**:全链路启动后,贯穿 FLOW-1 到 FLOW-5
- **系统行为**:把"10 账号 × 6 环节 × 3 平台"组织成有向任务图,按依赖关系调度
- **编排模型**:
  - **任务图**:每个 Content 走 热点→文案→脚本→混剪→分发→回评 串行链;不同 Content 之间并行
  - **并发控制**:单机同时最多 N 个 Playwright/Chrome 实例(N 默认 3,可配,超了内存爆)
  - **资源隔离**:每个账号独占一个 Chrome profile,不串 cookie
  - **失败隔离**:单账号/单环节失败不阻塞其他账号,失败任务进重试队列(最多 3 次),超限转人工
  - **断点续跑**:任务状态持久化(TaskRun 表),服务重启可恢复未完成任务
  - **调度策略**:
  - 选题→文案→脚本 可批量并发(AI 调用,IO 密集)
  - **FLOW-3 agent 剪辑**:
    - 抽帧 + LLM 看图理解:IO 密集,可并发多个 Content(受中转 API 配额限制)
    - 渲染(Remotion/FFmpeg):CPU 密集,串行排队或低并发(同时最多 N 个,N 默认 2)
    - 单个 Content 的 agent 流程是异步长任务,需状态持久化,服务重启可恢复
  - 分发按平台风控限速(同账号同平台两次发布间隔 ≥30 分钟)
- **MUST**:任务队列持久化;并发上限可配;失败重试 + 转人工;服务重启可续跑
- **技术选型建议**(留 DEV-PLAN 定):Celery / RQ / Dramatiq + Redis,或自写轻量队列

### 4.4 核心数据模型

### 4.4 核心数据模型

```
Account(账号)
  id, platform(枚举:小红书/抖音/快手/视频号), nickname, 
  topic_theme(主题), 
  auth_state(加密 cookie/登录态), status(启用/禁用), 
  created_at, updated_at

Topic(选题)
  id, source_platform, title, heat_score, 
  matched_account_ids[], status(候选/采纳/弃用), created_at

Content(内容)
  id, account_id, topic_id, 
  title, body, tags, video_script, 
  status(生成中/待审/已审/发布中/已发布/失败), 
  platform_post_url, created_at, published_at

Comment(评论)
  id, content_id, platform_comment_id, 
  author, text, ai_reply, 
  status(待回/已回/转人工), replied_at

TaskRun(任务运行记录)
  id, flow_type, account_id, content_id, 
  status, started_at, finished_at, error_log
```

---

## 5. AI 能力规格

### 5.1 litmus test
**AI 不是核心价值的全部,但拿掉 AI 这个产品不成立**——纯规则工具能做分发和采集,但文案生成、视频脚本、评论回复必须靠 AI。判定:**AI 必要。**

### 5.2 AI 能力选型(已搜 2026-07 当前最佳)
| 功能 | 选型 | 理由 |
|---|---|---|
| 文案生成 | 豆包 / 通义千问 API | 中文语感好,2026 价格战每万 token ~0.2 元 |
| 视频脚本 | 豆包 / 通义千问 API | 同上 |
| 评论回复 | 豆包 / 通义千问 API | 同上 |
| 视频混剪素材 | MoneyPrinterPlus 内置 | 现成轮子 |

### 5.3 概率性四问(每个 AI 功能必答)

#### 文案/脚本生成
- **质量条**:文案符合账号主题、平台调性,人工抽检通过率达标(用户自验,见下)
- **触发方式**:FLOW-1 选题确认后自动触发
- **失败兜底**:API 失败重试 3 次,仍失败标记待人工,不阻塞其他账号
- **延迟与成本**:单次生成 ≤15s;日均成本 [待补充,需实测调用量 × 单价]

#### 评论回复
- **质量条**:回复不跑题、语气符合人设,人工抽检通过率达标(用户自验)
- **触发方式**:发布后定时轮询新评论(频率待 Q1 实跑调试)
- **失败兜底**:生成失败 → 跳过不强行回,进待处理队列
- **延迟与成本**:单条 ≤10s;日均成本 [待补充]

> **质量验收说明**:原版本"≥80% 通过率"为编造数字且无法自动化验收。改为:上线后由用户人工抽检前 N 条,不达标则调优 prompt 并重测,N 和阈值由用户在 DEV 阶段自定。

---

## 6. UI 与状态

### 6.1 核心界面清单
| 屏 | 职责 |
|---|---|
| SCREEN-1 仪表盘 | 账号总览、今日任务、异常告警 |
| SCREEN-2 账号管理 | 矩阵 CRUD、登录态、主题配置 |
| SCREEN-3 内容流水线 | 选题→文案→视频→发布全链路状态 |
| SCREEN-4 评论中心 | 各账号评论与 AI 回复记录 |
| SCREEN-5 数据概览 | 基础数据回显(发布数/互动数) |
| SCREEN-6 日志与设置 | 任务日志、AI 配置、风控参数 |

### 6.2 五态覆盖(每个核心屏必须设计)

#### SCREEN-1 仪表盘 五态
- **加载态**:骨架屏 + "正在拉取账号状态…"
- **空态**:无账号 → 引导"先去账号管理添加你的第一个号"
- **错误态**:某账号登录态失效 → 红点 + "XX 账号需要重新登录"
- **成功态**:各号状态卡片 + 今日产出统计
- **无权限态**:本地服务未启动 → "请先启动后端服务"

#### SCREEN-3 内容流水线 五态
- **加载态**:各环节步骤条 + 转圈
- **空态**:今日尚未启动 → "点开始今日运营"CTA
- **错误态**:某环节失败 → 红色步骤 + 错误摘要 + 重试按钮
- **成功态**:绿色步骤链 + 各平台发布链接
- **无权限态**:无可用账号 → "请先配置账号"

(SCREEN-2/4/5/6 五态模式相同,详见 Design Brief 阶段补全)

---

## 7. 验收与边界

### 7.1 关键功能 Given-When-Then

#### AC-1 全链路一键跑通
- **Given** 已配置 ≥1 个有效账号、主题、登录态
- **When** 用户在仪表盘点"开始今日运营"
- **Then** 30 分钟内该账号在所启用平台有内容发布成功,评论开始自动回复

#### AC-2 多账号矩阵
- **Given** 已配置 10 个号、4 平台、多主题
- **When** 启动全链路
- **Then** 各号按各自主题生成不同内容,互不串号

#### AC-3 失败可恢复
- **Given** 某账号发布失败(风控/网络)
- **When** 用户查看异常
- **Then** 看到失败原因 + 可一键重试,不影响其他账号

#### AC-4 账号可删除(可建必可删)
- **Given** 矩阵中存在某账号
- **When** 用户删除该账号
- **Then** 本地:该账号所有内容、评论、任务记录一并清除,本地登录态失效
- **And** 提示用户:平台侧登录态需用户自行到该平台退出(本工具无法主动登出平台侧会话)

### 7.2 边界值
| 维度 | 边界 |
|---|---|
| 账号数 | 1-10(超 10 不保证性能) |
| 平台数 | 4(固定) |
| 单次发布并发 | ≤4 平台并行 |
| 评论轮询频率 | 每 10 分钟/号 |
| 视频时长 | 15s-3min |

### 7.3 非功能要求
| 类 | 要求 |
|---|---|
| 性能 | 单账号全链路端到端耗时待 POC-3 确定基线;面板首屏 ≤2s |
| 安全 | 登录态 cookie 加密存储;AI API Key 不落日志 |
| 隐私 | 所有数据本地存储,不上云;不采集用户隐私 |
| 兼容 | Chrome 最新版;Python 3.10+;Windows/macOS |
| 可靠 | 单账号失败不阻塞其他账号;任务可断点续跑 |
| 可访问 | Web 面板响应式(桌面优先;移动端适配 [待补充]) |

### 7.4 依赖(DEP)
| ID | 依赖 | 说明 |
|---|---|---|
| DEP-1 | **Remotion / HyperFrames** | agent 剪辑的渲染框架:LLM 写 HTML/组件 → 渲染成 MP4。B 站 Codex 剪视频同款路线 |
| DEP-2 | MediaCrawler(Pro 版优先) | 采集热榜+评论 |
| DEP-7 | Pexels/Pixabay API | 无版权视频/图片素材源(场景 B) |
| DEP-8 | Edge-TTS / 豆包 TTS / GPT-SoVITS | TTS 配音[选型待 A/B] |
| DEP-9 | Whisper | 语音识别→字幕时间轴对齐 |
| DEP-10 | FFmpeg | 抽帧 + 剪切拼接(场景 A 核心 + 场景 B 兜底) |
| **DEP-11** | **中转 LLM API(智谱 GLM / DeepSeek / 通义 / 豆包)** | **agent 大脑**:文本生成 + 多模态视觉(看抽帧)。用户指定用国内中转 |
| **DEP-12** | **视频剪辑 Skills(chengfeng-videocut-skills 等)** | **agent 的剪辑技能包**:标准化转场/字幕/卡点指令集,现成开源 |
| **DEP-13** | MoneyPrinterTurbo(47.5K star) | 场景 B 链路参考(降为参考,非主底座) |
| DEP-3 | Social-Auto-Upload / Spreado | 分发底层参考 |
| DEP-4 | 豆包/通义千问 API | AI 文案脚本评论 |
| DEP-5 | Chrome + Playwright | 浏览器自动化底座 |
| DEP-6 | 各平台有效账号 + 登录态 | 用户自备 |

### 7.5 假设与待确认

| ID | 假设/风险 | 依据 | 状态 |
|---|---|---|---|
| **A-1** | **用户明确接受自动分发+自动回评的封号风险** | 用户多轮明确选择"全自动含分发/回评" | 已确认,风险用户扛 |
| A-2 | MediaCrawler 开源版够用,不一定上 Pro | 矩阵 ≤10,开源版单机可跑 | 待 POC 验证 |
| A-3 | 豆包/通义千问 API 稳定性满足日跑 | 主流厂商 SLA | 待实跑观察 |
| A-4 | MoneyPrinterPlus 当前仍活跃维护 | 搜索显示持续更新,但 2026 活跃度信息有限 | 待 GitHub Release 核实 |
| **A-5** | **视频号因微信 2026 严打自动化政策,降级为半自动** | POC-1:微信 2026-03 新增"非真人自动化创作"条款,封号率 15% | **已定论,用户确认降级** |
| **A-6** | **MoneyPrinterTurbo(原 MoneyPrinterPlus)License 需核实,fork 二开存在潜在法律风险,用户已知悉接受** | v1.2 换底座;开源项目 License 需逐个核实 | **用户明确选择"直接 fork 不考虑 License",风险用户扛。自用风险低,若 SaaS 化/对外发布/上架则风险升高** |
| **A-7** | ~~v1 自建视频剪辑 agent~~ → **作废(v1.4)**,改为调现成 API | 用户澄清需求是"高光提取 + 从零生成",非自建 agent | 已纠正 |

### 7.7 POC-3 全链路手工基线耗时测量表(用户侧实测)

> 北极星指标的基线全靠这张表。请用户找一天,完全手动走一遍当前运营流程,如实填数字,不发我也要自己留底。填完回填到 2.2 成功度量。

| 环节 | 单条内容耗时 | 备注 |
|---|---|---|
| 查热点(筛出 1 个选题) | ___ 分钟 | |
| 写文案(1 篇) | ___ 分钟 | |
| 写视频脚本 | ___ 分钟 | |
| 剪视频(1 条) | ___ 分钟 | 最吃时间,认真填 |
| 分发到 3 平台(1 条,小红书+抖音+快手) | ___ 分钟 | |
| 分发到视频号(1 条) | ___ 分钟 | |
| 回评论(10 条) | ___ 分钟 | |
| **单条全链路合计** | **___ 分钟** | |
| × 目标日均条数 | × ___ | |
| = **日均总耗时** | **___ 小时** | |

填完发我,我重算 2.2 北极星和验收基线。

### 7.6 待确认问题(写进 Spec 但留给开发阶段解决)
- [ ] Q1: 各平台 2026-07 的风控阈值具体多少?(影响轮询频率,需实跑调试)
- [ ] Q2: 豆包 vs 通义千问,文案质量谁更好?(需 A/B 实测)
- [ ] Q3: MoneyPrinterPlus 最新 Release 是否兼容当前 Python 环境?
- [x] ~~Q4: 视频号发布可行性~~ → **已定论:降级半自动,见 SCOPE-1 / 假设 A-5**
- [ ] Q5: 全链路手工流程真实基线耗时(用户填 POC-3 测量表,见 7.7,决定北极星具体数字)
- [x] ~~Q6: L2 视频理解模型选型~~ → **作废(v1.4)**,改为调高光提取 API,不自建
- [x] ~~Q7: agent 框架选型~~ → **作废(v1.4)**,不自建 agent
- [ ] **Q8: 中转 API 多模态视觉能力验证(v1.5 更新)**——智谱 GLM-4V / 通义千问 VL / 豆包视觉,看抽帧理解视频内容够不够用?需 POC 验证准确率和成本(决定 FLOW-3 能不能跑)

---

## 8. 下一步

本 Spec 完成(已含 POC 修订),接下来:
1. **用户通读本 Spec v1.1**,确认 POC 结论落地无误
2. **用户填 POC-3 测量表(7.7)**,回填北极星基线
3. 可选:进 Design Brief(定视觉规范)或直接跳过
4. 进 DEV-PLAN:按 3 平台 × 功能拆 Phase,分批落地。建议拆法:Phase 1 单平台单账号跑通全链路(选风控最松的)→ Phase 2 加平台 → Phase 3 加矩阵 → Phase 4 视频号半自动

---

*本文件为单一真相源。任何需求变更先改这里 + 记 Changelog,再动代码。*
